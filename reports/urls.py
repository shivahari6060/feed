from django.urls import path

app_name = 'reports' # This name important for reverse lookup


urlpatterns = [
    # path('login/', views.login_view, name='login'),
    # path('register/', views.register_view, name='register'),
]